# OpenShare MVP Requirements

This repository implements the MVP described in `/Users/nanisamireddy/Downloads/requirements.md`.

OpenShare is a browser-based screen sharing app with no user accounts, no database, and direct peer-to-peer WebRTC media. The backend only creates rooms, tracks room state in memory, and relays Socket.IO signaling messages.
