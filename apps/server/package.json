{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@openshare/shared": "workspace:*",
    "@upstash/redis": "^1.38.0",
    "dotenv": "^17.2.3",
    "fastify": "^5.6.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "socket.io-client": "^4.8.1",
    "tsx": "^4.20.6",
    "vitest": "^4.0.14"
  }
}
